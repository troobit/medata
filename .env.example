# WebAuthn Authentication Configuration
# Copy this file to .env.local and fill in the values

# Relying Party ID - the domain name for WebAuthn
# Development: localhost
# Production: your-domain.com
AUTH_RP_ID=localhost

# Expected origin for WebAuthn verification
# Development: http://localhost:5173
# Production: https://your-domain.com
AUTH_ORIGIN=http://localhost:5173

# Path to credentials storage file (relative to project root)
# Only used for local development when Vercel KV is not configured
AUTH_CREDENTIALS_PATH=./data/credentials.json

# Vercel KV Configuration (Production)
# These environment variables are automatically set when you link a Vercel KV
# store to your project. Do NOT set these manually for local development.
# To set up Vercel KV:
#   1. Create a KV store: vercel storage add kv medata-auth
#   2. Link to project: vercel link && vercel env pull .env.local
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Session secret for signing session cookies
# Generate with: openssl rand -base64 32
# Must be at least 32 characters
AUTH_SESSION_SECRET=your-secret-key-at-least-32-characters-long

# Bootstrap token for initial credential enrollment
# Only required when setting up the application for the first time
# Generate with: openssl rand -hex 16 or use a strong passphrase
# Remove or unset after initial enrollment for security
AUTH_BOOTSTRAP_TOKEN=your-bootstrap-token-here

# ─────────────────────────────────────────────────────────────────────────────
# Vercel KV Configuration (Production)
# ─────────────────────────────────────────────────────────────────────────────
# These variables are auto-set by Vercel when you link a KV store to your project.
# Do NOT set these manually - they are populated automatically via:
#   vercel link && vercel env pull .env.local
#
# When KV_REST_API_URL is present, the app uses Vercel KV for credential storage.
# When absent, it falls back to file-based storage (AUTH_CREDENTIALS_PATH).
#
# KV_REST_API_URL=https://your-kv-store.kv.vercel-storage.com
# KV_REST_API_TOKEN=your-kv-token
