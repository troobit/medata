import SwiftUI

// Stub registered in task 12; the full-screen capture-error overlay (amber
// ghost outline, chip, hint, Retry / 2-view / Cancel) is built in task 15.
struct CaptureErrorOverlay: View {
    var body: some View { EmptyView() }
}
