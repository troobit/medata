import SwiftUI

// Stub registered in task 12; the capture-path fork sheet (Quick / Two-view,
// card toggle) is built in task 16.
struct LidarForkSheetView: View {
    var body: some View { EmptyView() }
}
