// HarnessCLI — offline test-set runner (design §7.3). Subcommands `accuracy`,
// `calibrate`, `seg-bench`, `calibrate-and-eval` land in tasks 56–64.
import Foundation
import Pipeline

print("HarnessCLI — Foundation skeleton. Subcommands land in tasks 56–64.")
