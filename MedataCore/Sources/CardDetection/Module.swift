// CardDetection module — Vision rect detection + P4P (design §3.2 / §6.1).
// Implementation lands in tasks 10 / 11.
import Foundation
import PortableContracts

public enum CardDetection {
    public static let moduleName = "CardDetection"
}
