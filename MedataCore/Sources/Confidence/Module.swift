// Confidence module — σ_meal combination per Req 13 (design §3.8 / §6.8).
// Implementation lands in tasks 39 / 40.
import Foundation
import PortableContracts

public enum ConfidenceModule {
    public static let moduleName = "Confidence"
}
