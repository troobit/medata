// Foods module — GRDB.swift over CoFID + AFCD (CoFID-wins merge; IFCDB overlay removed
// per Decision 39) (design §3.7 / §4.1 / §6.12).
import Foundation
import PortableContracts

public enum FoodsModule {
    public static let moduleName = "Foods"
}
