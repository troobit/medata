// Foods module — GRDB.swift over CoFID + IFCDB overlay (design §3.7 / §4.1 / §6.12).
// Implementation lands in tasks 35 / 36.
import Foundation
import PortableContracts

public enum FoodsModule {
    public static let moduleName = "Foods"
}
