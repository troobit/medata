// Macros module — per-class formulas + meal totals (design §3.8 / §6 macronutrient formulas).
import Foundation
import PortableContracts

public enum MacrosModule {
    public static let moduleName = "Macros"
}
