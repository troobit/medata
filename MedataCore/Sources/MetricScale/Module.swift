// MetricScale module — pure-function scale resolver and σ_s (design §3.4 / §6.4).
// Implementation lands in tasks 17 / 18.
import Foundation
import PortableContracts

public enum MetricScaleModule {
    public static let moduleName = "MetricScale"
}
