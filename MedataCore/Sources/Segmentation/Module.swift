// Segmentation module — Core ML wrapper, pre/post-processing, ownership (design §3.5 / §6.5).
// Implementation lands in tasks 19–23.
import Foundation
import PortableContracts

public enum SegmentationModule {
    public static let moduleName = "Segmentation"
}
