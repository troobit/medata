// SupportPlane module — RANSAC plane fit + iterative card-only fit (design §3.3 / §6.2 / §6.3).
// Implementation lands in tasks 13 / 14 / 15 / 16.
import Foundation
import PortableContracts

public enum SupportPlaneModule {
    public static let moduleName = "SupportPlane"
}
