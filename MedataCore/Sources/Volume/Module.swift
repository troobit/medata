// Volume module — Metal voxel carve + height-field integration (design §3.6 / §6.6 / §6.7).
// Implementation lands in tasks 24–34.
import Foundation
import PortableContracts

public enum VolumeModule {
    public static let moduleName = "Volume"
}
