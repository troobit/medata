# Wireframes (reference inputs)

Pasted Claude artifacts land here, one file per screen (e.g. `result-view.html`).
These are visual source-of-truth inputs, not shipped code — nothing imports them.
See `docs/agent-notes/wireframe-intake.md` for how each becomes a design-system
page + requirements.
