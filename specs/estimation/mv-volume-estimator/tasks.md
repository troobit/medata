# Minimum Viable Volume Estimator

## Closure

- [x] 1. Spec closed without implementation (Decision 5)
  - No tasks were ever written and no code landed. Decision 5 invalidated the smolspec's root-cause premise on a static read: StubInferenceEngine paints white_rice at ~0.99999 in both views
  - so the per-class carve already had a carveable silhouette and decoupling the segmenter solved a non-problem
  - The two-view noFoodVolumeRecovered symptom was re-diagnosed in specs/bugfixes/two-view-carve-no-volume/ as a mis-aimed oblique capture with an unwired tilt aim guide — since wired; only its on-device verification remains
  - This file exists so the closure is legible to the outstanding-work sweep: without it the folder reads as design-complete-tasks-missing forever
