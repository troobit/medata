torch>=2.4
torchvision>=0.19
coremltools>=8.0
ai-edge-torch>=0.2
tensorflow>=2.16
pillow>=10.0
numpy>=1.26

# SegFormer-only dependency — the conversion spike (spike_segformer.py,
# segmenter-foundation design §5.2) and the registered segformer_b0
# architecture (archs.py, task 23). Not needed for deeplab_mnv3
# train/export/validation; imported lazily.
transformers>=4.44

# dev/test only — the export-gate and lineage unit tests (tools/segmenter/tests/)
# run without torch/coremltools installed.
pytest>=8.0
