torch>=2.4
torchvision>=0.19
coremltools>=8.0
ai-edge-torch>=0.2
tensorflow>=2.16
pillow>=10.0
numpy>=1.26

# spike-only dependency — spike_segformer.py (segmenter-foundation design §5.2)
# loads a public SegFormer-B0 checkpoint for the Core ML conversion spike.
# Not needed for train/export/validation.
transformers>=4.44

# dev/test only — the export-gate and lineage unit tests (tools/segmenter/tests/)
# run without torch/coremltools installed.
pytest>=8.0
